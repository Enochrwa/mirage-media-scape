{
  "name": "zovyra-native",
  "version": "0.1.0",
  "main": "index.js",
  "types": "index.d.ts",
  "napi": {
    "name": "zovyra-native",
    "triples": {
      "defaults": true,
      "additional": []
    }
  },
  "license": "MIT",
  "devDependencies": {
    "@napi-rs/cli": "^2.18.4",
    "@types/node": "^25.8.0",
    "typescript": "^5.4.5"
  },
  "scripts": {
    "artifacts": "napi artifacts",
    "build": "npx tsx scripts/build-native.ts",
    "build:napi": "napi build --platform --release && npm run build:stub",
    "build:debug": "napi build --platform && npm run build:stub",
    "build:stub": "tsc --project tsconfig.stub.json",
    "version": "napi version"
  }
}
